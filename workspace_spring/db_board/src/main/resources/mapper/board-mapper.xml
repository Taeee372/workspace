<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.db_board.mapper.BoardMapper">
    <resultMap id="board" type="com.green.db_board.dto.BoardDTO">
        <id     column="BOARD_NUM"      property="boardNum"/>
        <result column="TITLE"          property="title"/>
        <result column="WRITER"         property="writer"/>
        <result column="CONTENT"        property="content"/>
        <result column="READ_CNT"       property="readCnt"/>
        <result column="CREATE_DATE"    property="createDate"/>
    </resultMap>

    <!--BoardList-->
	<select id="getBoardAll" resultMap="board">
        SELECT BOARD_NUM
            , TITLE
            , WRITER
            , CREATE_DATE
            , READ_CNT
        FROM BOARD
        ORDER BY BOARD_NUM DESC
    </select>

    <!--RegForm-->
    <insert id="regBoard">
        INSERT INTO BOARD(
            TITLE
            , WRITER
            , CONTENT
        ) VALUES (
            #{title}
            , #{writer}
            , #{content}
        )
    </insert>

    <!--BoardDetail-->
    <select id="getBoard" resultMap="board">
        SELECT BOARD_NUM
            , CREATE_DATE
            , WRITER
            , READ_CNT
            , TITLE
            , CONTENT
        FROM BOARD
        WHERE BOARD_NUM = #{boardNum}
    </select>

    <!--UpdateForm-->
    <update id="updateBoard">
        UPDATE BOARD
        SET
            TITLE = #{title}
            , CONTENT = #{content}
        WHERE BOARD_NUM = #{boardNum}
    </update>

    <!--DeleteBoard-->
    <delete id="deleteBoard">
        DELETE FROM BOARD
        WHERE BOARD_NUM = #{boardNum}
    </delete>

    <!--제목 기준 게시글 검색-->
    <select id="searchTitle" resultMap="board">
        SELECT BOARD_NUM
            , CREATE_DATE
            , WRITER
            , READ_CNT
            , TITLE
            , CONTENT
        FROM BOARD
        WHERE TITLE LIKE CONCAT('%', #{title}, '%')
    </select>

    <!--작성자 기준 게시글 검색-->
    <select id="searchWriter" resultMap="board">
        SELECT BOARD_NUM
        , CREATE_DATE
        , WRITER
        , READ_CNT
        , TITLE
        , CONTENT
        FROM BOARD
        WHERE WRITER LIKE CONCAT('%', #{writer}, '%')
    </select>
</mapper>































